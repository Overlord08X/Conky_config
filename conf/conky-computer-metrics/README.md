# conky-computer-metrics

Deskripsi: Menampilkan metrik komputer (CPU, memori, disk, network).

Dependencies:
- `conky`
- utilitas bawaan sistem (`df`, `free`, `top`/`ps`, dsb.)

Usage:

```bash
conky -c ~/.config/conky/conf/conky-computer-metrics/conky-computer-metrics &
```
